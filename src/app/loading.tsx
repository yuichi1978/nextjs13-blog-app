const Loading = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <section className="w-16 h-16 border-t-4 border-gray-500 rounded-full animate-spin"></section>
    </div>
  );
};

export default Loading;
